import React, { Fragment } from 'react';

const Redirect = () => {
    return (
        <Fragment>
            <h4 className="text-red">Reload Tidak Tersedia, silahkan kembali ke halaman sebelumnya</h4>
        </Fragment>
    );
};

export default Redirect;
